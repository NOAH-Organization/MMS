# NOAH Manga Management System (NOAH MMS) — Database Design

**Last revised:** 14/05/2026 05:30 PM

Based on the functional requirements and the anticipated operational needs of the system, MMS uses a relational database as its primary data store. AI session and message state (§1.1.4 · UC-05-01–05) are persisted in a separate NoSQL database managed by the AI agent memory layer and are not modeled here.

---

## Data General Requirements

Fields marked with `*` are **required** — they must not be null and, where applicable, their foreign key relationships must be satisfied. Fields marked **`ro` (read-only)** may not be modified after the record is created.

For primitive data types (excluding booleans), the default value is `null` unless a specific default is stated, signifying the absence of data. Count and total fields default to `0` unless stated otherwise. The system does not need high-precision decimal arithmetic, so `float` is the appropriate type for decimal values, rounded to 2 decimal places. Fields that represent image data (cover images, page images, etc.) must store a URL pointing to where the image is hosted.

Every entity must have a tracking `id` using the database's latest native UUID implementation. Every entity must also carry management metadata: `created_at`, `updated_at`, `deleted_at`, and `is_deleted` (defaults to `false`). The system supports **soft-delete** — hard deletion through the web interface is not supported. These metadata fields are managed by the system and are not directly controlled by users. All datetime fields must follow the format `DD-MM-YYYY hh:mm:ss` or `DD-MM-YYYY`.

Entities that represent manga content (series, chapters, volumes) must contain fields covering the following aspects: **identification**, **content information**, **publication information**, and **user statistics**. Note that some publication fields may appear similar to content fields but carry different meanings — for example, "total published chapters" (official release data) vs. "highest chapter number currently in the system" (system data). The current chapter and volume counts in the system must be computed automatically. Similarly, user statistics are aggregated from user interactions (comments, ratings, reads, etc.) and are also computed automatically.

For entity relationships: **many-to-many** relationships require a separate junction table storing both foreign keys. **One-to-many** and **zero-to-many** relationships require the "many" side to store the foreign key of the other entity.

---

## Conceptual Design

### Entity Definitions

| Category | Name | Description | Business Logic | Traceability |
| --- | --- | --- | --- | --- |
| **Strong Entity** | `Series` | Entity representing a reading series. | The main archived entity in the system. Can be suspended by an administrator without permanent deletion. | §1.1.1 · §1.2.1 · UC-01-01–05, UC-01-12, UC-04-01–07, UC-06-01–10 |
| | `Personnel` | Entity representing a user or administrator account. | Applicable for both normal personnel and administrators. All accounts are administrator-provisioned; self-registration is not supported. | §1.1.2 · §1.2.2 · UC-02-02–07, UC-03-01–14, UC-07-01–09 |
| | `Creator` | Entity representing an author or artist. | One person can be an author, an artist, or both. At least one role is required. | §1.1.1 · §1.1.3 · UC-01-03, UC-04-01, UC-04-02 |
| | `Tags` | Entity representing a series tag. | Tags categorize series into genres, themes, demographics, and more. Tag suggestions are generated by an external VLM service and require administrator approval before being applied. | §1.1.3 · §1.2.3 · D#5 · UC-04-03, UC-06-10, UC-08-02 |
| | `Translation Group` | Entity representing a group of translators. | Users who translate together are grouped into a translation group. Official publishers are also modeled as groups (`is_official_publisher = true`). A personnel member belongs to at most one group at a time. | §1.1.1 · §1.1.5 · UC-01-02, UC-06-01, UC-06-02, UC-06-11 |
| | `Post` | Entity representing user-generated content on the activity feed. | A post may embed a reference to a series, chapter, or specific page. A post can be reposted with optional commentary. | §1.1.2 · UC-03-01–05 |
| | `Scrape Job` | Entity representing a content ingestion job triggered by an administrator. | Can be one-off or recurring (cron schedule). Tracks execution state, result summary, and error details. Job history is retained for the content dashboard. | §1.2.1 · UC-06-01, UC-06-06, UC-06-07, UC-06-08 |
| | `Audit Log` | Append-only record of administrator actions. **OTEL-only** — not stored in the main database. | Permanent; not subject to soft-delete. Records actor identity, action type, affected entity, timestamp, and source IP. | §2.1 item 7 · UC-06-01–10, UC-07-01–09 |
| | `User Log` | Append-only record of user activity events. **OTEL-only** — not stored in the main database. | Permanent; not subject to soft-delete. Covers authentication events and content interactions. Required for per-personnel activity summaries. | §1.2.2 · UC-07-09 |
| **Weak Entity** | `Volume` | Entity representing a volume. Depends on `Series`. | A volume is identified by its series. A default Volume 0 acts as a catch-all for chapters with no assigned volume. | §1.1.1 · UC-01-02, UC-01-05, UC-06-01, UC-06-02 |
| | `Chapter` | Entity representing a chapter. Depends on `Series` and `Volume`. | A chapter is identified by its series and volume (including Volume 0). Multiple translations of the same chapter number may exist. | §1.1.1 · §1.2.1 · UC-01-01, UC-01-02, UC-01-06, UC-06-01, UC-06-02 |
| | `Character` | Entity representing a named character. Depends on `Series`. | A character is scoped to exactly one series and identified by it. Character profiles are viewable by all personnel. | §1.1.1 · UC-01-04 |
| | `Comment` | Entity representing a user comment. Depends on one of `Series`, `Chapter`, or `Post`. | Exactly one parent FK must be non-null per row. Supports one level of threading via a parent comment reference. | §1.1.2 · UC-03-04 |
| | `User Preference` | Entity representing per-user reading and display settings. Depends on `Personnel`. | Exactly one record per user; created with defaults at account provisioning. Persists reading direction, page display mode, and page fit across sessions and devices. | UC-01-07, UC-01-08, UC-01-09 |
| | `Token` | Entity representing an authentication token. Depends on `Personnel`. | Covers two lifecycles: `refresh` (session continuity) and `password_reset` (single-use). Stored as hashes only; never as plaintext. | §2.1 items 4–5 · UC-02-02, UC-02-03 |
| | `Notification` | Entity representing an in-app notification delivered to a user. Depends on `Personnel`. | Types: `friend_request`, `post_reply`, `direct_message`, `new_chapter`. Carries a JSONB reference payload for deep-linking to the triggering entity. | §1.1.2 · UC-03-11 |
| | `Message` | Entity representing a single direct message. Depends on `Conversation`. | Text-only in the initial release. Immutable once written. | §1.1.2 · UC-03-09, UC-03-10 |
| **Associated Entity** | `Series Creator Credit` | Association between `Series` and `Creator` carrying a credit role. | Role is one of `author` (writing) or `artist` (illustration); one person may hold both on the same series. A series must have at least one credit. | §1.1.1 · §1.1.3 · UC-01-03, UC-04-01, UC-04-02 |
| | `Series User Record` | Association representing a user's full relationship with a series. Links `Personnel` and `Series`. | One row per `(user_id, series_id)` pair, created on first chapter read and never deleted. Carries `is_bookmarked`, `notify_on_new_chapter`, and `rating`. `notify_on_new_chapter` may only be `true` while `is_bookmarked = true`; clearing the bookmark auto-clears the flag. | §1.1.1 · D#11 · UC-01-01, UC-01-06, UC-01-12, UC-03-11 |
| | `Chapter Rating` | Association representing a user's star-scale rating for a chapter. Links `Personnel` and `Chapter`. | Same structure as `Series Rating`. Feeds chapter-level and volume-level rating aggregates. | §1.1.1 |
| | `Post Vote` | Association representing a user's binary vote on a post. Links `Personnel` and `Post`. | Carries `direction` (upvote / downvote). At most one vote per user per post; changing direction updates in place. Feeds `Post.upvote_count` and `Post.downvote_count`. | §1.1.2 · D#1 · UC-03-05 |
| | `Creator Follow` | Association representing a user following a creator. Links `Personnel` and `Creator`. | Pure junction. Enables personalized recommendations based on followed creators and artists. | §1.1.3 · UC-04-05 |
| | `Tag Proposal` | Association between `Series` and `Tags` representing a pending tag application. | Carries `status` (pending / approved / rejected) and `source` (manual / ai). Required for the AI-tag review gate before publication. Approved proposals become active series–tag associations. | §1.2.3 · D#5 · UC-06-10, UC-08-02 |
| | `Reading Progress` | Intersection entity tracking a user's reading state within a specific chapter. Links `Personnel` and `Chapter`. | Carries `last_page`, `is_completed`, and `last_read_at`. Created on first page view; never deleted. `is_completed` transitions `false → true` and is never reset. | §1.1.1 · UC-01-06, UC-03-13 |
| | `Page Bookmark` | Intersection entity anchoring a user-placed bookmark to a specific page. Links `Personnel`, `Chapter`, and a page number. | Logical key: `(user_id, chapter_id, page_number)`. Carries an optional `note`. Distinct from the series-level `is_bookmarked` field in `Series User Record`. | §1.1.1 · UC-01-10 |
| | `Friend Request` | Self-referential directed association between two `Personnel` instances. | Status lifecycle: `pending → accepted / rejected / blocked`. A mutual `accepted` constitutes a friendship. A blocked pair cannot exchange messages or new requests. | §1.1.2 · UC-03-06, UC-03-07, UC-03-08 |
| | `Conversation` | Association representing a direct-message channel between exactly two `Personnel` instances. | One channel per unordered user pair. Only allowed between confirmed friends or when one participant is an administrator. | §1.1.2 · D#2 · UC-03-09, UC-03-10 |
| | `Character Appearance` | Pure junction between `Chapter` and `Character`. | Records which named characters appear in which chapters within a series. No extra attributes. | §1.1.1 · UC-01-04 |

### Relationship Definitions

#### Binary Relationships

| Entity A | Cardinality | Entity B | Associated Entity | Notes |
| --- | --- | --- | --- | --- |
| `Series` | one-to-many | `Volume` | — | FK on `Volume.series_id`; a series must have at least one volume, including default Vol 0 |
| `Series` | zero-to-many | `Chapter` | — | FK on `Chapter.series_id`; chapters are optional at the series level |
| `Series` | one-to-many | `Character` | — | FK on `Character.series_id`; characters are scoped to one series |
| `Series` | many-to-many | `Creator` | `Series Creator Credit` | Carries `role` (`author` for writing, `artist` for illustration); one person may hold both roles on the same series; a series must have at least one `author` credit |
| `Series` | many-to-many | `Tags` | `Tag Proposal` | Active association when `Tag Proposal.status = approved` |
| `Series` | many-to-many | `Personnel` | `Series User Record` | One row per `(user_id, series_id)` pair; created on first chapter read; carries `is_bookmarked`, `notify_on_new_chapter` (requires `is_bookmarked = true`), and `rating` (float, nullable) |
| `Volume` | zero-to-many | `Chapter` | — | FK on `Chapter.volume_id`; Vol 0 is the default container for unassigned chapters |
| `Chapter` | many-to-one | `Translation Group` | — | FK on `Chapter.group_id`; each chapter has exactly one translating group |
| `Chapter` | many-to-one | `Personnel` | — | FK on `Chapter.uploader_id`; the administrator or group member who uploaded the chapter; non-admin uploaders must have `Personnel.group_id` matching `Chapter.group_id` |
| `Chapter` | many-to-many | `Character` | `Character Appearance` | Characters appearing in a given chapter |
| `Chapter` | many-to-many | `Personnel` | `Chapter Rating` | Star-scale rating; one per user per chapter; updates replace in place |
| `Chapter` | many-to-many | `Personnel` | `Reading Progress` | Per-chapter reading state; carries `last_page`, `is_completed`, `last_read_at` |
| `Chapter` | many-to-many | `Personnel` | `Page Bookmark` | Page-level bookmarks; logical key `(user_id, chapter_id, page_number)` |
| `Translation Group` | one-to-many | `Personnel` | — | FK on `Personnel.group_id`; a personnel member belongs to at most one group at a time; `null` if not currently a member of any group |
| `Personnel` | one-to-one | `User Preference` | — | FK on `User Preference.user_id`; reading and display settings |
| `Personnel` | one-to-many | `Token` | — | FK on `Token.user_id`; refresh and password-reset token lifecycle |
| `Personnel` | one-to-many | `Notification` | — | FK on `Notification.recipient_id` |
| `Personnel` | one-to-many | `Post` | — | FK on `Post.author_id`; authorship |
| `Personnel` | one-to-many | `Comment` | — | FK on `Comment.author_id`; authorship |
| `Personnel` | one-to-many | `Scrape Job` | — | FK on `Scrape Job.created_by`; admin-only |
| `Personnel` | one-to-many | `Audit Log` | — | Logical reference on `actor_id`; OTEL-only, no DB-enforced FK |
| `Personnel` | one-to-many | `User Log` | — | Logical reference on `user_id`; OTEL-only, no DB-enforced FK |
| `Personnel` | many-to-many | `Creator` | `Creator Follow` | Follow relationship; enables personalized recommendations |
| `Personnel` | many-to-many | `Personnel` | `Friend Request` | Self-referential; status lifecycle: `pending → accepted / rejected / blocked` |
| `Personnel` | many-to-many | `Personnel` | `Conversation` | Symmetric DM channel; at most one channel per unordered user pair |
| `Post` | many-to-one (self) | `Post` | — | FK on `Post.original_post_id`; `null` unless `is_repost = true` |
| `Post` | one-to-many | `Comment` | — | FK on `Comment.post_id`; nullable — exactly one of the three `Comment` target FKs is non-null |
| `Post` | many-to-many | `Personnel` | `Post Vote` | Binary vote; carries `direction` (upvote / downvote); at most one per user per post |
| `Comment` | many-to-one (self) | `Comment` | — | FK on `Comment.parent_comment_id`; nullable; threading limited to one level |
| `Comment` | many-to-one | `Series` | — | FK on `Comment.manga_id`; nullable — exactly one of the three target FKs is non-null per row |
| `Comment` | many-to-one | `Chapter` | — | FK on `Comment.chapter_id`; nullable |
| `Comment` | many-to-one | `Post` | — | FK on `Comment.post_id`; nullable |
| `Conversation` | many-to-one | `Personnel` | — | FK on `participant_a_id` and `participant_b_id`; symmetric; exactly two participants per conversation |
| `Conversation` | one-to-many | `Message` | — | FK on `Message.conversation_id` |
| `Message` | many-to-one | `Personnel` | — | FK on `Message.sender_id` |
| `Scrape Job` | zero-to-one | `Series` | — | FK on `Scrape Job.target_manga_id`; `null` for new-title ingestion |

#### Multi-Entity Relationships

Relationships that cannot be expressed as a simple two-column FK or pure junction table — either because they involve three or more entity types simultaneously, or because the junction carries its own attributes.

| Participants | Associated Entity | Attributes | Notes |
| --- | --- | --- | --- |
| `Personnel` × `Series User Record` × `Series` | `Series User Record` | `is_bookmarked` (boolean, default `false`), `notify_on_new_chapter` (boolean, default `false`), `rating` (float, nullable) | Row created on first chapter read; never deleted. `notify_on_new_chapter` may only be `true` while `is_bookmarked = true`; clearing `is_bookmarked` auto-clears the flag. `rating` updates replace in place. Feeds `Series.total_reads`, `Series.total_bookmarks`, `Series.avg_rating`, `Series.total_ratings`. |
| `Personnel` × `Chapter Rating` × `Chapter` | `Chapter Rating` | `rating` (float) | One per user per chapter; updating replaces in place. Feeds `Chapter.avg_rating`, `Chapter.total_ratings`; aggregated into `Volume.avg_rating`, `Volume.total_ratings`. |
| `Personnel` × `Post Vote` × `Post` | `Post Vote` | `direction` (enum: `upvote` / `downvote`) | At most one vote per user per post; changing direction updates in place. Feeds `Post.upvote_count`, `Post.downvote_count`. |
| `Personnel` × `Reading Progress` × `Chapter` | `Reading Progress` | `last_page`, `is_completed` (default `false`), `last_read_at` | Created on first page view; never deleted. `is_completed` transitions `false → true`, never reset. Series completion = `is_completed = true` count ÷ total chapters. Feeds `Chapter.total_reads`, `Volume.total_reads`, `Personnel.total_chapters_read`, `Personnel.total_titles_read`. |
| `Personnel` × `Page Bookmark` × `Chapter` | `Page Bookmark` | `page_number`, `note` (optional), `bookmarked_at` | Logical key: `(user_id, chapter_id, page_number)`. One bookmark per page per user; placing on the same page replaces the note. No soft-delete. Feeds `Chapter.total_bookmarks`, `Volume.total_bookmarks`. |
| `Personnel` × `Friend Request` × `Personnel` | `Friend Request` | `status` (pending / accepted / rejected / blocked), `requested_at`, `responded_at` | Directed; single row per ordered pair. At most one active request per ordered pair; reverse direction also blocked while active. `blocked` prevents further contact. `rejected` allows re-submission after a cooldown. |
| `Personnel` × `Conversation` × `Personnel` | `Conversation` | `last_message_at` | Symmetric; participants carry no hierarchy. At most one channel per unordered pair. Requires confirmed friendship or one participant being an administrator. `last_message_at` updated atomically on each `Message` insert. |
| `Personnel` × `Comment` × (`Series` \| `Chapter` \| `Post`) | `Comment` | `body`, `parent_comment_id` (nullable) | Polymorphic — exactly one of `manga_id`, `chapter_id`, `post_id` is non-null per row. `parent_comment_id` must reference a `Comment` targeting the same parent entity. Threading limited to one level. Feeds `total_comments` on the target entity. |
| `Series` × `Chapter` × `Volume` × `Translation Group` | `Chapter` (tri-parent weak entity) | `series_original_title` (denormalized ro), `volume_number` (denormalized ro) | All three parent FKs are non-nullable; a `Chapter` cannot exist without all three. Denormalized fields are copied from read-only parent fields to avoid joins at display time. |

---

## Logical Design

### Schema definitions

#### Strong Entities

##### Manga (Series)

A manga series must carry complete information for display, advanced search, and management purposes. A series may have multiple authors, multiple artists, and zero or more chapters and volumes. Both the **original-language title** (e.g. Japanese for manga) and an **English title** are required. The original-language title must be written in its native script — for example, `ドラえもん` rather than the romanized form `Doraemon`. A series can be read, commented on, rated, and bookmarked by multiple users.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [§1.2.1](REQUIREMENTS.md) · [SS-01 (UC-01-01–12)](USECASES.md) · [SS-04 (UC-04-01–07)](USECASES.md) · [SS-06 (UC-06-01–10)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*original_title` | — | ro — title in the work's source language (Japanese for manga, Chinese for manhua, Korean for manhwa, etc.) |
| | `*english_title` | — | for search convenience |
| | `vietnamese_title` | — | |
| | `alt_titles` | — | aliases, abbreviations, alternate names |
| **Content Info** | `description` | — | plot summary |
| | `cover_image` | — | URL |
| | `background_image` | — | URL |
| | `highest_chapter` | — | highest chapter number currently in the system (auto-computed) |
| | `highest_volume` | — | highest volume number currently in the system (auto-computed) |
| **Publication Info** | `*status` | — | one of: `upcoming`, `ongoing`, `finished`, `hiatus`, `cancelled` — default `upcoming` |
| | `latest_chapter` | — | latest officially released chapter number |
| | `latest_volume` | — | latest officially released volume number |
| | `start_year` | — | |
| | `end_year` | — | |
| | `country` | — | |
| | `platforms` | — | platforms the series is available on |
| | `series_type` | — | one of: `manga` (Japan), `manhua` (China), `manhwa` (Korea), `comic` (other) — default `manga` |
| | `chapter_type` | — | one of: `oneshot`, `series`, `anthology` — default `series` |
| | `content_rating` | — | one of: `everyone`, `teen`, `mature`, `explicit` — governs content-filter visibility |
| **Moderation** | `is_suspended` | — | default `false` — administrator-imposed visibility override; hides the title from all personnel without permanent deletion |
| **User Stats** | `avg_rating` | `Series User Record` | auto-computed — average of `rating` across rows where `rating IS NOT NULL` |
| | `total_ratings` | `Series User Record` | auto-computed — count of rows where `rating IS NOT NULL` |
| | `total_reads` | `Series User Record` | auto-computed — distinct user count (all rows for this series) |
| | `total_comments` | `Series × Comment` | auto-computed from `Comment` rows where `manga_id` matches |
| | `total_bookmarks` | `Series User Record` | auto-computed — count of rows where `is_bookmarked = true` |
| **Relationships** | `Volume` | `Series × Volume` | one-to-many — FK on `Volume.series_id`; a series must have at least one volume, including default Vol 0 |
| | `Chapter` | `Series × Chapter` | zero-to-many — FK on `Chapter.series_id`; chapters are optional at the series level |
| | `Character` | `Series × Character` | one-to-many — FK on `Character.series_id`; characters are scoped to one series |
| | `Creator` | `Series Creator Credit` | many-to-many — carries `role` (`author` for writing, `artist` for illustration); one person may hold both roles on the same series; a series must have at least one `author` credit |
| | `Tags` | `Tag Proposal` | many-to-many — active when `Tag Proposal.status = approved`; proposals may be `source: manual` or `source: ai` |
| | `Personnel` | `Series User Record` | many-to-many — one row per `(user_id, series_id)` pair; created on first chapter read; never deleted; carries `is_bookmarked` (default `false`), `notify_on_new_chapter` (default `false`, requires `is_bookmarked = true`), and `rating` (float, nullable); feeds `total_reads`, `total_bookmarks`, `avg_rating`, `total_ratings` |
| | `Comment` | `Series × Comment` | one-to-many — polymorphic; no junction table; referenced by `Comment.manga_id` (nullable); exactly one of the three `Comment` target FKs is non-null per row; feeds `total_comments` |

---

##### Tags

Tags provide metadata for categorization and advanced search. A series can have many tags; a tag can belong to many series. Tags may also belong to different groups, enabling deeper filtering.

> **Traceability:** [§1.1.3](REQUIREMENTS.md) · [§1.2.3](REQUIREMENTS.md) · [SS-04 (UC-04-03)](USECASES.md) · [SS-06 (UC-06-10)](USECASES.md) · [SS-08 (UC-08-02)](USECASES.md) · Decision [#5](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | |
| | `*name` | — | tag or genre name, e.g. `Slice of Life`, `Romance`, `Shounen` |
| **Content Info** | `description` | — | explanation of what the tag means |
| | `group` | — | the category this tag belongs to, e.g. theme (fantasy, sci-fi), character type (villainess), narrative style (isekai), genre (action, romance), demographic (shounen, shoujo) |
| **Relationships** | `Series` | `Tag Proposal` | many-to-many — active when `Tag Proposal.status = approved`; proposals may be `source: manual` or `source: ai` |

---

##### Creator (Author / Artist)

Stores personal information and works for authors and artists. A single person can be both an author and an artist, only an author, or only an artist — at least one role is required. The pen name the person uses publicly should be used as the display name rather than their legal name.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [§1.1.3](REQUIREMENTS.md) · [SS-01 (UC-01-03)](USECASES.md) · [SS-04 (UC-04-01, UC-04-02)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | |
| | `*pen_name` | — | the name or pseudonym this person uses publicly |
| | `native_name` | — | name in their native language/script |
| | `alt_names` | — | list of other aliases |
| **Personal Info** | `bio` | — | brief biography and works overview |
| | `birth_date` | — | |
| | `death_date` | — | |
| | `nationality` | — | |
| | `gender` | — | |
| | `social_links` | — | key-value map of social media URLs (X/Twitter, Pixiv, Facebook, etc.) |
| **Work Info** | `total_works_official` | — | total number of works they have created — may not match the system count |
| | `total_works_in_system` | `Series Creator Credit` | auto-computed — count of series credited to this creator in the system |
| | `is_author` | — | whether this person is credited as an author |
| | `is_artist` | — | whether this person is credited as an artist |
| **Relationships** | `Series` | `Series Creator Credit` | many-to-many — carries `role` (`author` for writing, `artist` for illustration); one person may hold both roles on the same series |
| | `Personnel` | `Creator Follow` | many-to-many — users can follow this creator; enables personalized recommendations |

---

##### Translation Group

A translation group is the team responsible for translating a chapter. Each chapter is attributed to exactly one group. Official publisher releases are treated as a group where `is_official_publisher` is `true`. Members of a group are optionally tracked via a junction to User.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [§1.1.5](REQUIREMENTS.md) · [SS-01 (UC-01-02, UC-01-03)](USECASES.md) · [SS-06 (UC-06-11)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | |
| | `*name` | — | |
| | `alt_names` | — | list of alternate names or abbreviations |
| **Info** | `description` | — | |
| | `website_url` | — | URL |
| | `languages` | — | list of languages this group works in |
| | `is_official_publisher` | — | marks an official publisher (e.g. Viz Media, Yen Press) — default `false` |
| **Stats** | `total_chapters_translated` | `Translation Group × Chapter` | auto-computed — count of chapters attributed to this group |
| **Relationships** | `Chapter` | `Translation Group × Chapter` | one-to-many — FK on `Chapter.group_id`; each chapter has exactly one translating group |
| | `Personnel` | `Translation Group × Personnel` | one-to-many — FK on `Personnel.group_id`; personnel currently belonging to this group; a personnel member belongs to at most one group at a time and may leave or join at any time |

---

##### User / Personnel

A user account represents one personnel member or administrator. All accounts are administrator-provisioned — self-registration is not supported. The `password_hash` field is write-only and must never be returned by any API endpoint. Reading statistics visibility is configurable per user.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [§1.2.2](REQUIREMENTS.md) · [§2.1](REQUIREMENTS.md) · [SS-02 (UC-02-02–07)](USECASES.md) · [SS-07 (UC-07-01–09)](USECASES.md) · Decision [#8](REQUIREMENTS.md) · Decision [#9](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*username` | — | ro — unique; used for login only, not the display name |
| | `*email` | — | ro — unique; used for login and password reset |
| **Account Info** | `display_name` | — | shown on profile; defaults to `username` |
| | `password_hash` | — | ro — bcrypt (min cost 12) or Argon2id; write-only, never exposed via API |
| | `avatar_url` | — | URL |
| | `bio` | — | |
| | `*role` | — | one of: `personnel`, `administrator` — default `personnel` |
| | `*account_status` | — | one of: `active`, `suspended`, `deleted` — default `active` |
| | `reading_stats_privacy` | — | one of: `public`, `friends_only` — default `public` |
| | `last_active_at` | — | updated on every successful login and authenticated request |
| | `group_id` | `Translation Group` | nullable — FK on `Personnel.group_id`; the translation group this personnel currently belongs to; `null` if not a member of any group; mutable — updated when personnel leave or join a group |
| **Security** | `is_2fa_enabled` | — | default `false` |
| | `2fa_method` | — | one of: `authenticator_app`, `sms` — `null` if 2FA is disabled |
| | `phone_number` | — | stored encrypted — required when `2fa_method = sms`; `null` otherwise; never exposed via API |
| | `2fa_secret` | — | stored encrypted — `null` if 2FA is disabled; never exposed via API |
| | `failed_login_attempts` | — | default `0`; reset on successful login |
| | `locked_until` | — | `null` if not locked; auto-set after exceeding the failed-attempt threshold |
| **Activity Stats** | `total_titles_read` | `Reading Progress` | auto-computed |
| | `total_chapters_read` | `Reading Progress` | auto-computed |
| **Relationships** | `Series` | `Series User Record` | many-to-many — reading, bookmark, and rating state per series; one record per `(user_id, series_id)` pair |
| | `Chapter` (upload) | `Personnel × Chapter` | one-to-many — FK on `Chapter.uploader_id`; chapters this user has uploaded |
| | `Chapter` (progress) | `Reading Progress` | one-to-many — per-chapter reading state; carries `last_page`, `is_completed`, `last_read_at` |
| | `Chapter` (page bookmark) | `Page Bookmark` | one-to-many — page-level bookmarks within chapters |
| | `Post` (authored) | `Personnel × Post` | one-to-many — FK on `Post.author_id` |
| | `Post` (vote) | `Post Vote` | many-to-many — binary upvote/downvote; at most one per user per post |
| | `Comment` | `Personnel × Comment` | one-to-many — FK on `Comment.author_id` |
| | `Creator` | `Creator Follow` | many-to-many — enables personalized recommendations |
| | `Translation Group` | `Personnel × Translation Group` | many-to-one — FK on `Personnel.group_id`; nullable; mutable — personnel may leave their current group and join a different one at any time |
| | `Personnel` (friend) | `Friend Request` | many-to-many (self-referential) — status lifecycle: `pending → accepted / rejected / blocked` |
| | `Personnel` (message) | `Conversation` | many-to-many (self-referential) — symmetric DM channel; at most one per unordered pair |
| | `Notification` | `Personnel × Notification` | one-to-many — FK on `Notification.recipient_id` |
| | `AI Session` | `Personnel × AI Session` | one-to-many — FK on `AI Session.user_id` |
| | `Scrape Job` | `Personnel × Scrape Job` | one-to-many — FK on `Scrape Job.created_by`; admin-only |
| | `Token` | `Personnel × Token` | one-to-many — FK on `Token.user_id`; refresh and password-reset lifecycle |
| | `User Preference` | `Personnel × User Preference` | one-to-one — FK on `User Preference.user_id`; reading and display settings |

---

##### Post

A post is a piece of user-generated content shared on the activity feed. A post may optionally embed a reference to a manga title, chapter, or a specific page. A repost is a post that points to an existing post; its own `body` acts as optional commentary.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-01, UC-03-02, UC-03-03, UC-03-05)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Content** | `*body` | — | text content of the post |
| | `embedded_reference` | — | JSONB — optional structured reference: `{ type, id, page_number? }` where type is one of `manga`, `chapter`, `page` |
| | `is_repost` | — | default `false` |
| | `original_post_id` | `Post × Post` | ro — `null` if not a repost; references the original post |
| **Stats** | `upvote_count` | `Post Vote` | auto-computed |
| | `downvote_count` | `Post Vote` | auto-computed |
| | `comment_count` | `Post × Comment` | auto-computed |
| | `repost_count` | `Post × Post` | auto-computed — count of reposts referencing this post |
| **Relationships** | `Personnel` (author) | `Personnel × Post` | many-to-one — FK on `Post.author_id` |
| | `Personnel` (vote) | `Post Vote` | many-to-many — binary vote; at most one per user per post; carries `direction` (upvote / downvote) |
| | `Post` (original) | `Post × Post` | many-to-one (self-referential) — `null` unless `is_repost = true` |
| | `Comment` | `Post × Comment` | one-to-many — FK on `Comment.post_id`; comments targeting this post |

---

##### Scrape Job

Represents a content ingestion job triggered by an administrator — either a one-off scrape from a URL or a scheduled recurring job. Job history is retained for auditing and the content dashboard (UC-06-08).

> **Traceability:** [§1.2.1](REQUIREMENTS.md) · [SS-06 (UC-06-01, UC-06-06, UC-06-07, UC-06-08)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Configuration** | `*source_url` | — | ro |
| | `target_manga_id` | `Scrape Job × Series` | ro — `null` if this job creates a new title; references an existing series if updating |
| | `is_recurring` | — | default `false` |
| | `schedule` | — | cron expression — `null` if `is_recurring = false` |
| **Execution** | `*status` | — | one of: `pending`, `running`, `completed`, `failed`, `cancelled` — default `pending` |
| | `started_at` | — | `null` until the job begins |
| | `completed_at` | — | `null` until the job ends |
| | `result_summary` | — | JSONB — e.g. `{ chapters_added, pages_added, errors }` |
| | `error_details` | — | JSONB — `null` if no errors |
| **Relationships** | `Personnel` | `Personnel × Scrape Job` | many-to-one — FK on `Scrape Job.created_by`; the administrator who initiated the job |
| | `Series` | `Scrape Job × Series` | zero-to-one — FK on `Scrape Job.target_manga_id`; `null` for new-title ingestion |

---

##### Audit Log

> **Observability note:** `AuditLog` records are **not stored in the main application database**. They are emitted as structured telemetry events to the observability stack (OpenTelemetry / OTEL) and consumed by a dedicated logging backend. This entity definition describes the event schema only — it has no ORM model or migration in the main service.

An append-only record of administrator actions that create, modify, or delete data. Audit log entries are permanent and exempt from soft-delete. The `metadata` field captures a before/after snapshot of affected fields where applicable.

> **Traceability:** [§2.1](REQUIREMENTS.md) (item 7) · [SS-07 (UC-07-01–09)](USECASES.md) · [SS-06 (UC-06-01–10)](USECASES.md)
> Note: Audit logs are append-only and permanent. The soft-delete fields and management metadata described in General Requirements do not apply to this entity.

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Event** | `*actor_id` | `Personnel × Audit Log` | ro — the administrator who performed the action |
| | `*action_type` | — | ro — e.g. `create_manga`, `delete_user`, `suspend_account`, `assign_role`, `force_password_reset` |
| | `*affected_entity_type` | — | ro — e.g. `manga`, `user`, `chapter`, `scrape_job` |
| | `*affected_entity_id` | — | ro |
| | `*timestamp` | — | ro |
| | `*source_ip` | — | ro |
| | `metadata` | — | JSONB — before/after snapshot of changed fields; `null` if not applicable |
| **Relationships** | `Personnel` (actor) | `Personnel × Audit Log` | many-to-one — logical reference on `actor_id`; OTEL-only, no DB-enforced FK; always an administrator |

---

##### User Log

> **Observability note:** `UserLog` records are **not stored in the main application database**. They are emitted as structured telemetry events to the observability stack (OpenTelemetry / OTEL) and consumed by a dedicated logging backend. This entity definition describes the event schema only — it has no ORM model or migration in the main service.

An event record capturing user activity on the system. Covers both authentication events (login, logout, failed login) and content interactions (read, rate, comment, bookmark). The `metadata` payload carries action-specific context. Records are append-only and permanent; the soft-delete fields and management metadata described in General Requirements do not apply to this entity.

> **Traceability:** [§1.2.2](REQUIREMENTS.md) · [SS-07 (UC-07-09)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*user_id` | `Personnel × User Log` | ro |
| **Event** | `*action_type` | — | ro — one of: `login`, `logout`, `read_chapter`, `rate_manga`, `rate_chapter`, `comment`, `bookmark_series`, `bookmark_page`, `password_reset_request`, `upload_chapter` |
| | `*timestamp` | — | ro — set by the system on emission |
| | `*success` | — | ro — `true` if the action completed successfully; `false` for failed attempts (e.g. failed login) |
| **Context** | `source_ip` | — | ro |
| | `user_agent` | — | ro |
| | `metadata` | — | ro — JSONB; action-specific context, e.g. `{ series_id, chapter_id }` for read events |
| **Relationships** | `Personnel` | `Personnel × User Log` | many-to-one — logical reference on `user_id`; OTEL-only, no DB-enforced FK |

---

#### Weak Entities

##### Volume

A volume must carry complete information for display, advanced search, and management. A volume may contain zero or more chapters, but belongs to exactly one series. Every series has a default **Volume 0**, which acts as a catch-all for chapters that do not belong to any known published volume ("No Volume"). Volume 0 is the default assignment for any newly added chapter.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-02, UC-01-05)](USECASES.md) · [SS-06 (UC-06-01, UC-06-02)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*volume_number` | — | ro |
| | `title` | — | optional volume title |
| **Publication Info** | `release_date` | — | |
| | `publisher` | — | default `"unknown"` |
| | `isbn` | — | International Standard Book Number |
| | `edition` | — | e.g. anniversary, collab, standard — default `"standard"` |
| | `published_chapter_start` | — | first chapter number in the official release |
| | `published_chapter_end` | — | last chapter number in the official release |
| | `store_links` | — | online stores where this volume is available |
| | `price` | — | key-value map of prices in different currencies |
| **Content Info** | `description` | — | context and plot summary for this volume |
| | `cover_image` | — | front cover URL |
| | `current_chapter_start` | — | first chapter currently in the system for this volume |
| | `current_chapter_end` | — | last chapter currently in the system for this volume |
| | `total_pages` | — | total page count currently in the system |
| **User Stats** | `avg_rating` | `Chapter Rating` | auto-computed — average rating across all chapters in this volume |
| | `total_ratings` | `Chapter Rating` | auto-computed — count of ratings across all chapters in this volume |
| | `total_reads` | `Reading Progress` | auto-computed — distinct user count across all chapters in this volume |
| | `total_comments` | `Chapter × Comment` | auto-computed from `Comment` rows where `chapter_id` matches any chapter in this volume |
| | `total_bookmarks` | `Page Bookmark` | auto-computed from `Page Bookmark` records across all chapters in this volume |
| **Relationships** | `Series` | `Volume × Series` | many-to-one — FK on `Volume.series_id`; carries denormalized `series_original_title` (ro) to avoid joins at display time |
| | `Chapter` | `Volume × Chapter` | zero-to-many — FK on `Chapter.volume_id`; Vol 0 is the default container for unassigned chapters |

---

##### Chapter

A chapter must carry complete information for display, advanced search, and management. Every chapter must belong to exactly one series and exactly one volume (Volume 0 is the default). Multiple translations of the same chapter number may exist — different languages or multiple translations in the same language. Each chapter may only be translated by one group, or it may be an official release (in which case the publisher is considered the translation group). Users can read, comment on, rate, and bookmark chapters.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [§1.1.5](REQUIREMENTS.md) · [§1.2.1](REQUIREMENTS.md) · [SS-01 (UC-01-01, UC-01-02, UC-01-06)](USECASES.md) · [SS-06 (UC-06-01, UC-06-02, UC-06-11)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*chapter_number` | — | ro |
| | `title` | — | optional chapter title |
| **Publication Info** | `release_date` | — | |
| | `*language` | — | language of this chapter version — can be the source language (Japanese, Korean, Chinese) or a translated language (English, Vietnamese); default `"none"` (for wordless chapters) |
| | `is_official` | — | whether this is an official translation — default `false` |
| **Content Info** | `description` | — | chapter summary |
| | `*page_count` | — | always a positive integer; every chapter must have at least one page |
| | `is_special` | — | marks anniversary or special end-of-volume chapters — default `false` |
| | `*pages` | — | JSONB array of page objects, each containing metadata such as order, URL, and dimensions |
| **User Stats** | `avg_rating` | `Chapter Rating` | auto-computed from `Chapter Rating` junction |
| | `total_ratings` | `Chapter Rating` | auto-computed from `Chapter Rating` junction |
| | `total_reads` | `Reading Progress` | auto-computed — distinct user count in `Reading Progress` for this chapter |
| | `total_comments` | `Chapter × Comment` | auto-computed from `Comment` rows where `chapter_id` matches |
| | `total_bookmarks` | `Page Bookmark` | auto-computed from `Page Bookmark` records for this chapter |
| **Relationships** | `Series` | `Chapter × Series` | many-to-one — FK on `Chapter.series_id`; carries denormalized `series_original_title` (ro) |
| | `Volume` | `Chapter × Volume` | many-to-one — FK on `Chapter.volume_id`; carries denormalized `volume_number` (ro); Vol 0 as default |
| | `Translation Group` | `Chapter × Translation Group` | many-to-one — FK on `Chapter.group_id`; for official releases the publisher is recorded as the group |
| | `Personnel` (uploader) | `Personnel × Chapter` | many-to-one — FK on `Chapter.uploader_id`; the administrator or group member who uploaded this chapter; non-admin uploaders must have `Personnel.group_id` matching `Chapter.group_id` |
| | `Personnel` (progress) | `Reading Progress` | many-to-many — per-chapter reading state; carries `last_page`, `is_completed`, `last_read_at` |
| | `Personnel` (rating) | `Chapter Rating` | many-to-many — star-scale rating; one per user per chapter; updates replace in place |
| | `Personnel` (page bookmark) | `Page Bookmark` | many-to-many — page-level bookmarks; logical key `(user_id, chapter_id, page_number)` |
| | `Character` | `Character Appearance` | many-to-many — characters appearing in this chapter |
| | `Comment` | `Chapter × Comment` | one-to-many — polymorphic; referenced by `Comment.chapter_id` (nullable); feeds `total_comments` |

##### Character

A character entity stores profile information for named characters appearing in a series. Characters are scoped to a single series, though they may appear across multiple chapters within it. Character pages are viewable by all personnel.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-04)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*name` | — | primary display name |
| | `aliases` | — | list of alternate names or nicknames |
| **Description** | `role` | — | one of: `protagonist`, `antagonist`, `supporting`, `other` |
| | `description` | — | character background and role in the story |
| | `image_url` | — | URL |
| **Relationships** | `Series` | `Character × Series` | many-to-one — FK on `Character.series_id`; carries denormalized `series_original_title` (ro); a character belongs to exactly one series |
| | `Chapter` | `Character Appearance` | many-to-many — chapters in which this character appears |

---

##### User Preference

Stores per-user reading and display settings. Exactly one record exists per user; it is created with default values when the account is provisioned. Settings persist across sessions and devices.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-07, UC-01-08, UC-01-09)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × User Preference` | ro — primary key; one-to-one with Personnel |
| **Reading** | `reading_direction` | — | one of: `rtl`, `ltr` — default `rtl` |
| | `page_display_mode` | — | one of: `single`, `double_spread`, `long_strip` — default `single` |
| | `page_fit` | — | one of: `fit_width`, `fit_height`, `original` — default `fit_width` |
| **Relationships** | `Personnel` | `Personnel × User Preference` | one-to-one — FK on `User Preference.user_id` |

---

##### Token

A general-purpose token record covering two lifecycles: `refresh` tokens (used to obtain new access tokens without re-authentication) and `password_reset` tokens (single-use, authorises one password change via an emailed link). Tokens are stored as hashes — the plaintext is generated once, returned to the caller, and never persisted. Records are invalidated by setting `revoked_at` rather than being deleted. The soft-delete fields and management metadata described in General Requirements do not apply to this entity.

> **Traceability:** [§2.1](REQUIREMENTS.md) (items 4–5) · [SS-02 (UC-02-02, UC-02-03)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*user_id` | `Personnel × Token` | ro |
| | `*type` | — | ro — one of: `refresh`, `password_reset` |
| **Content** | `*token_hash` | — | ro — hash of the plaintext token; plaintext is never stored |
| **Lifecycle** | `*issued_at` | — | ro — set by the system on insert |
| | `*expires_at` | — | absolute expiry timestamp |
| | `revoked_at` | — | `null` if still valid; set on use (for `password_reset`), rotation (for `refresh`), or explicit revocation |
| **Relationships** | `Personnel` | `Personnel × Token` | many-to-one — FK on `Token.user_id`; a user may hold multiple active tokens of different types |

---

##### Friend Request

Represents a directed friendship request between two users. A mutual `accepted` status constitutes a friendship. The `blocked` status prevents further contact in either direction.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-06, UC-03-07, UC-03-08)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*requester_id` | `Personnel × Friend Request` | ro — the user who initiated the request |
| | `*addressee_id` | `Personnel × Friend Request` | ro — the user who received the request |
| **State** | `*status` | — | one of: `pending`, `accepted`, `rejected`, `blocked` — default `pending` |
| | `requested_at` | — | ro |
| | `responded_at` | — | `null` until the addressee acts |
| **Relationships** | `Personnel` (requester) | `Personnel × Friend Request` | many-to-one — FK on `requester_id` |
| | `Personnel` (addressee) | `Personnel × Friend Request` | many-to-one — FK on `addressee_id` |

---

##### Comment

A comment can target a manga series, a chapter, or a post. Exactly one of `manga_id`, `chapter_id`, or `post_id` must be non-null. Comments support one level of threading via `parent_comment_id`.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-04)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Content** | `*body` | — | text content |
| | `parent_comment_id` | `Comment × Comment` | `null` for top-level; references parent comment for replies; threading limited to one level |
| **Target** | `manga_id` | `Series × Comment` | `null` if not targeting a series; exactly one of the three target FKs is non-null per row |
| | `chapter_id` | `Chapter × Comment` | `null` if not targeting a chapter |
| | `post_id` | `Post × Comment` | `null` if not targeting a post |
| **Relationships** | `Personnel` (author) | `Personnel × Comment` | many-to-one — FK on `Comment.author_id` |

---

##### Conversation

A direct-message thread between exactly two users. Messaging is restricted to mutually confirmed friends and administrators (see Decision #2). The initial release supports text-only messages.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-09, UC-03-10)](USECASES.md) · Decision [#2](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **State** | `last_message_at` | `Conversation × Message` | auto-updated on each new message insert |
| **Relationships** | `Personnel` | `Personnel × Conversation` | many-to-one — FK on `participant_a_id` and `participant_b_id`; symmetric; exactly two participants per conversation |
| | `Message` | `Conversation × Message` | one-to-many — FK on `Message.conversation_id` |

---

##### Message

An individual message within a Conversation. `sent_at` is equivalent to `created_at` and is set by the system on insert.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-09, UC-03-10)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Content** | `*body` | — | text content — text only in the initial release |
| **State** | `*is_read` | — | default `false` |
| | `*sent_at` | — | ro — set by the system on insert |
| **Relationships** | `Conversation` | `Conversation × Message` | many-to-one — FK on `Message.conversation_id` |
| | `Personnel` (sender) | `Personnel × Message` | many-to-one — FK on `Message.sender_id` |

---

##### Notification

An in-app notification delivered to a user. The `reference` field is a JSONB payload carrying enough data to deep-link to the triggering entity without a separate query.

> **Traceability:** [§1.1.2](REQUIREMENTS.md) · [SS-03 (UC-03-11)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| **Content** | `*type` | — | one of: `friend_request`, `post_reply`, `direct_message`, `new_chapter` |
| | `*is_read` | — | default `false` |
| | `reference` | — | JSONB — e.g. `{ type: "chapter", id: "...", series_title: "..." }` |
| **Relationships** | `Personnel` (recipient) | `Personnel × Notification` | many-to-one — FK on `Notification.recipient_id` |

---

##### Reading Progress

Tracks a user's reading state for a specific chapter. Series-level completion percentage is computed from this table (chapters completed ÷ total chapters). `last_read_at` is updated on every read event.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-06)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × Reading Progress` | ro — composite primary key with `chapter_id` |
| | `*chapter_id` | `Chapter × Reading Progress` | ro |
| **State** | `*last_page` | — | the most recent page the user viewed |
| | `*is_completed` | — | default `false`; set to `true` when the user reaches the last page; never reset |
| | `last_read_at` | — | updated on every read event |
| **Relationships** | `Personnel` | `Personnel × Reading Progress` | many-to-one — FK on `user_id` |
| | `Chapter` | `Chapter × Reading Progress` | many-to-one — FK on `chapter_id` |

---

##### Page Bookmark

Records a user-placed bookmark on a specific page within a chapter (UC-01-10). This is distinct from the series-level bookmark (User → Manga many-to-many). An optional `note` allows the user to annotate the bookmark.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-10)](USECASES.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*id` | — | ro |
| | `*user_id` | `Personnel × Page Bookmark` | ro |
| | `*chapter_id` | `Chapter × Page Bookmark` | ro |
| | `*page_number` | — | ro — the specific page that was bookmarked |
| **Content** | `note` | — | optional user annotation |
| | `bookmarked_at` | — | ro — set by the system on insert |
| **Relationships** | `Personnel` | `Personnel × Page Bookmark` | many-to-one — FK on `user_id` |
| | `Chapter` | `Chapter × Page Bookmark` | many-to-one — FK on `chapter_id` |

---

#### Associated Entities

Associated entities implement many-to-many relationships and may carry additional attributes. Each has a composite primary key formed from the foreign keys of the entities it connects (plus any discriminating attributes).

---

##### Series Creator Credit

Links a `Series` to a `Creator` with a discriminating `role`. Because the same creator can hold multiple roles on the same series (rare but valid), `role` is part of the composite primary key.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · Relationship Definitions

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*series_id` | `Series Creator Credit × Series` | ro — composite PK with `creator_id` and `role` |
| | `*creator_id` | `Series Creator Credit × Creator` | ro |
| | `*role` | — | ro — one of: `author`, `artist` |
| **Relationships** | `Series` | `Series Creator Credit × Series` | many-to-one — FK on `series_id` |
| | `Creator` | `Series Creator Credit × Creator` | many-to-one — FK on `creator_id` |

---

##### Series User Record

Captures the full relationship between a user (`Personnel`) and a series. One row per `(user_id, series_id)` pair, created on first chapter read and never deleted. Carries bookmark state, notification preference, and rating in a single row to avoid cross-table constraints.

> **Traceability:** [§1.1.1](REQUIREMENTS.md) · [SS-01 (UC-01-06, UC-01-12)](USECASES.md) · [SS-03 (UC-03-11)](USECASES.md) · Decision [#11](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × Series User Record` | ro — composite PK with `series_id` |
| | `*series_id` | `Series × Series User Record` | ro |
| **State** | `is_bookmarked` | — | default `false` |
| | `notify_on_new_chapter` | — | default `false`; may only be `true` while `is_bookmarked = true`; clearing `is_bookmarked` auto-clears this flag |
| | `rating` | — | float, nullable; updated in place; feeds `Series.avg_rating` and `Series.total_ratings` |
| **Relationships** | `Personnel` | `Personnel × Series User Record` | many-to-one — FK on `user_id` |
| | `Series` | `Series × Series User Record` | many-to-one — FK on `series_id` |

---

##### Chapter Rating

Records a user's rating for a specific chapter. One row per `(user_id, chapter_id)` pair; updates replace in place.

> **Traceability:** [§1.1.1](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × Chapter Rating` | ro — composite PK with `chapter_id` |
| | `*chapter_id` | `Chapter × Chapter Rating` | ro |
| **Content** | `*rating` | — | float; typically 1.0–5.0 |
| **Relationships** | `Personnel` | `Personnel × Chapter Rating` | many-to-one — FK on `user_id` |
| | `Chapter` | `Chapter × Chapter Rating` | many-to-one — FK on `chapter_id` |

---

##### Post Vote

Records a user's vote on a post. One row per `(user_id, post_id)` pair; updates replace the existing direction.

> **Traceability:** [§1.1.2](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × Post Vote` | ro — composite PK with `post_id` |
| | `*post_id` | `Post × Post Vote` | ro |
| **Content** | `*direction` | — | one of: `upvote`, `downvote` |
| **Relationships** | `Personnel` | `Personnel × Post Vote` | many-to-one — FK on `user_id` |
| | `Post` | `Post × Post Vote` | many-to-one — FK on `post_id` |

---

##### Creator Follow

Records a user following a creator. Pure junction with no additional attributes; presence of a row implies following.

> **Traceability:** [§1.1.1](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*user_id` | `Personnel × Creator Follow` | ro — composite PK with `creator_id` |
| | `*creator_id` | `Creator × Creator Follow` | ro |
| **Relationships** | `Personnel` | `Personnel × Creator Follow` | many-to-one — FK on `user_id` |
| | `Creator` | `Creator × Creator Follow` | many-to-one — FK on `creator_id` |

---

##### Tag Proposal

Links a `Series` to a `Tags` entry and tracks proposal lifecycle (`status`) and origin (`source`). A tag is considered active on a series when `status = approved`.

> **Traceability:** [§1.1.1](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*series_id` | `Series × Tag Proposal` | ro — composite PK with `tag_id` |
| | `*tag_id` | `Tags × Tag Proposal` | ro |
| **State** | `*status` | — | one of: `pending`, `approved`, `rejected`; default `pending` |
| | `*source` | — | one of: `manual`, `ai` |
| **Relationships** | `Series` | `Series × Tag Proposal` | many-to-one — FK on `series_id` |
| | `Tags` | `Tags × Tag Proposal` | many-to-one — FK on `tag_id` |

---

##### Character Appearance

Records that a character appears in a specific chapter. Pure junction with no additional attributes.

> **Traceability:** [§1.1.1](REQUIREMENTS.md)

| Category | Field | Associated Entity / Relationship | Notes |
| --- | --- | --- | --- |
| **Identification** | `*chapter_id` | `Chapter × Character Appearance` | ro — composite PK with `character_id` |
| | `*character_id` | `Character × Character Appearance` | ro |
| **Relationships** | `Chapter` | `Chapter × Character Appearance` | many-to-one — FK on `chapter_id` |
| | `Character` | `Character × Character Appearance` | many-to-one — FK on `character_id` |
